# Narrativas — Sistema de Gestão de Evento Acadêmico

Plataforma exclusiva do evento "Narrativas Interculturais, Decoloniais e Antirracistas em Educação" (GPDES/UnB). Substitui o Even3. Evento anual, multi-edição (V, VI, VII...) — todo o modelo de dados deve suportar múltiplas edições desde já.

## Stack (não negociável)

- **Frontend:** Next.js (App Router), **JavaScript puro + JSX**. **PROIBIDO TypeScript** (nenhum `.ts`/`.tsx`, nenhum `tsconfig.json`).
- **Estilos:** **SCSS Modules** (`*.module.scss`). **PROIBIDO Tailwind**, styled-components ou CSS-in-JS.
- **Componentes da área interna:** **PrimeReact** (`primereact@10`, preso na v10 porque a v11 exige React 19 e o projeto está em React 18/Next.js 14) em modo `unstyled` (`PrimeReactProvider value={{ unstyled: true }}`, ver `(interno)/layout.jsx`/`(onboarding)/layout.jsx`), estilizado via `pt` (passthrough) com classes de `*.module.scss` batendo os tokens do `DESIGN.md` — nunca o tema visual padrão do PrimeReact. Migração em andamento: `EdicaoForm` já usa (`CampoTexto`/`CampoNumero`/`CampoArea`/`Button`/`Toast`); `Botao`, `Campo`/`CampoSelect`/`CampoSenha`/`CampoCPF`/`CampoFoto`, `Modal`, `Alerta`, o dropdown do `MenuConta` e o seletor do `EdicaoControles` ainda são hand-rolled em SCSS Modules puro e serão migrados progressivamente. O site público (`(publico)`) não usa PrimeReact, exceto o `Stepper`/`StepperPanel` do fluxo de inscrição (`(publico)/inscricao`), também em modo `unstyled`+`pt`, estilizado com os tokens públicos.
- **Backend:** Express (Node.js, JavaScript).
- **Banco:** PostgreSQL + **Prisma** (`backend/prisma/schema.prisma`).
- **Auth:** JWT em cookie httpOnly (access curto + refresh), senha com bcrypt.
- **E-mail:** API HTTP do Resend (`RESEND_API_KEY` no `.env`; dev sem chave: Ethereal via Nodemailer, link de preview no console). Trocado de SMTP puro porque o Railway bloqueia portas de saída 25/465/587 em todos os planos — qualquer SMTP tradicional trava até estourar timeout em produção.

Antes de propor qualquer alternativa a essa stack (outra lib de estilo, TypeScript, outro ORM etc.), pare e pergunte.

## Estrutura do repositório

```
narrativas/
├── frontend/        # Next.js — porta 3000
│   ├── app/
│   │   ├── (publico)/    # identidade visual do evento (stencil/terroso)
│   │   └── (interno)/    # participante/admin — visual limpo e dessaturado
│   ├── components/
│   ├── styles/            # _tokens-publico.scss, _tokens-interno.scss, globals.scss
│   └── lib/                # apiClient, validação, helpers
└── backend/          # Express — porta 4000
    ├── prisma/schema.prisma
    └── src/
        ├── routes/ controllers/ services/ middlewares/ validators/ utils/
        └── server.js
```

## Identidade visual — duas peles

- **Site público** (`(publico)`, inclui login/cadastro): editorial, stencil, paleta terrosa (`--tinta`, `--barro`, `--ocre`, `--areia`, `--papel`, `--cerrado`). Tipografia display stencil (Allerta Stencil / Saira Stencil One) + Archivo. Ver `frontend/styles/_tokens-publico.scss`.
- **Área interna** (`(interno)`: participante/admin): limpa, silenciosa, mesma família terrosa dessaturada (`--fundo`, `--superficie`, `--borda`, `--texto`, `--acento`). Ver `frontend/styles/_tokens-interno.scss`. Hierarquia por espaçamento/peso/cor — nunca por caixas coloridas.

Nunca misture os tokens das duas peles entre si.

## Convenções

- Antes de criar ou alterar qualquer tela, leia e siga o `DESIGN.md`. Em caso de conflito com instruções antigas, o `DESIGN.md` vence.
- Código (variáveis, funções, nomes de arquivo, commits de schema) em **inglês** onde for genérico de infraestrutura; **nomes de domínio do negócio** (models Prisma, rotas, mensagens) em **português**, já que refletem o vocabulário do próprio evento (ex.: `Usuario`, `/auth/cadastro`, `PapelUsuario`). Segue o que já foi estabelecido nas rotas e no schema — não traduza para inglês.
- Toda UI, mensagens de erro/validação e e-mails em **português**.
- Estilos sempre via `*.module.scss` importado no componente; nada de estilo inline exceto casos pontuais (ex.: valor dinâmico).
- Middlewares de auth: `autenticar` (valida JWT do cookie) e `autorizar(...papeis)`.
- Mensagens de auth não devem vazar existência de conta em login/recuperação de senha (mensagens genéricas). Cadastro pode ser específico (UX exige saber que e-mail/CPF já existe).
- Exclusão de conta é anonimização (LGPD), nunca `DELETE` físico do usuário — preserva integridade referencial para módulos futuros.
- **Toda página nova ou alterada deve ser responsiva**, incluindo a área interna (que prioriza desktop mas não pode quebrar no celular — ver `DESIGN.md`). Verificar shell (sidebar/topbar), tabelas e formulários em telas estreitas antes de dar a tela por pronta.
- **Toda mutação (criar/editar/excluir) na área interna mostra um toast de sucesso ou erro** via `useToast()` (`frontend/components/interno/ToastProvider.jsx`), além da validação inline por campo já usada nos formulários. Requisições de leitura (GET) não precisam de toast — vira ruído. Convenção vale só pra área interna por enquanto; o site público não foi migrado.
- **Toda confirmação de ação (excluir, remover, promover etc.) na área interna usa `ModalConfirmacao`** (`frontend/components/interno/ModalConfirmacao.jsx`) — nunca um estado inline que substitui os botões de ação na própria linha da tabela/lista.

## Commits

Pequenos e frequentes, mensagens em português, estilo Conventional Commits: `feat:`, `fix:`, `chore:`, `docs:`, `refactor:`. Exemplo: `feat: cadastro de usuário com confirmação de e-mail`.

## Mapa dos módulos

**Fase 1 (até dez/2026 — V edição):**

1. Cadastro, login e autenticação — **concluído**
2. Painel administrativo (edições e atividades) — iniciado (CRUD de edições pronto; organizadores prontos — papel global (vale para todas as edições), convite por e-mail para quem ainda não tem conta, e agora com permissões granulares por seção do admin (`SecaoAdmin`, campos `acessoCompleto`/`secoesPermitidas` em `Usuario`): ADMIN sempre tem acesso total; um ORGANIZADOR só acessa o que estiver marcado (ou tudo, se `acessoCompleto`); enforcement em todas as rotas admin existentes via middleware `autorizarSecao` (`backend/src/middlewares/autorizarSecao.js`) e no menu lateral (`NavegacaoEdicao.jsx` só mostra o que o usuário pode acessar); gestão unificada na tela `/admin/edicoes/[id]/participantes` (antiga tela `/organizadores` foi removida — convidar, promover a admin, remover e editar permissões, tudo ADMIN-only mesmo para quem só tem a seção "Participantes" liberada, pra evitar autopromoção; a mesma tela também tem "Alterar e-mail de usuário", que busca qualquer `Usuario` da base — não só organizadores/admins — via `BuscaUsuario`/`GET /usuarios/busca` e atualiza o e-mail em `PATCH /usuarios/:id/email`, já marcando `emailConfirmado: true` sem exigir novo fluxo de confirmação; essa ação, diferente das demais da tela, não é ADMIN-only — está liberada por `autorizarSecao("PARTICIPANTES")`, então um ORGANIZADOR com a seção "Participantes" também pode usá-la); CRUD de atividades pronto — nome, slug auto-gerado (fixo após criação), descrição, carga horária, local, tipo de atividade obrigatório, lista de pessoas envolvidas (nome, foto opcional, descrição, tipo livre como facilitador/mediador/apresentador), sem exclusão de atividade com inscritos; vagas e período (data/hora de início e fim, "exige inscrição", "sem limite de participantes") ficam direto na atividade — modelo de sessões (múltiplas ocorrências por atividade) foi removido por simplicidade, cada atividade tem uma ocorrência única; CRUD de tipos de atividade pronto — catálogo global, compartilhado entre todas as edições, sem exclusão de tipo em uso; personalização da página pública da edição (`/admin/edicoes/[id]/pagina`) iniciada — seção Hero com logo customizável por edição pronta (o gestor envia um `.svg` colorido, sanitizado no backend com DOMPurify+jsdom via `sanitizarSvgLogo.js` e injetado inline em `Logo.jsx`, reaproveitando o efeito de reveal "rolo de tinta"; camada branca é derivada por CSS a partir da mesma arte); as cores da logo enviada também ficam editáveis depois, sem reenviar o arquivo — o sanitizador extrai os `fill`/`stroke`/`stop-color` distintos (até 12), reescreve pra `var(--logo-cor-N)` e guarda o mapa em `logoSvgCores` (Json), editável no admin via seletor de cor nativo por cor detectada (`CampoCoresLogo.jsx`); Hero também com cor de fundo, cor de texto e cor do búzio customizáveis independentemente — fundo reusa a paleta de 5 tokens de `corFundoRealizadores` com opacidade ajustável (0-100, mistura com `--papel` via CSS `color-mix()`) ou pode virar uma imagem full-bleed (um arquivo por breakpoint — 1920×1080 recomendado no desktop, 1080×1920 no mobile, `background-size:cover`, mesmo breakpoint 1000px da faixa); texto/búzio usam uma paleta pública mais ampla (inclui `--buzio`/`--areia`) via novo enum `CorPublica`; aviso de contraste (WCAG, não bloqueante) no admin quando a combinação escolhida fica abaixo do mínimo do DESIGN.md (considera a opacidade do fundo no cálculo); faixas decorativas fixas nas laterais da tela completam a Hero — configuráveis separadamente pra desktop e mobile (cor sólida, imagem enviada pelo gestor — raster comum via GCS, repete verticalmente e aceita PNG/WebP com transparência — ou nenhuma faixa; `faixaHeroTipoDesktop`/`corFaixaHeroDesktop`/`imagemFaixaHeroDesktop` e os mesmos três com sufixo `Mobile`, breakpoint 1000px), já que a faixa é bem mais estreita no mobile; navbar (`BarraNavegacao`) ajusta o próprio padding pela largura da faixa via `--largura-faixa-hero` compartilhada; seção Apoio (`/admin/edicoes/[id]/pagina/apoio`) espelha Realizadores 1:1 (mesmo modelo `EdicaoApoiador`, mesmos campos de cor/opacidade/faixa e mesma UI), renderizada na home logo acima de Realização; seção Localização (`/admin/edicoes/[id]/pagina/localizacao`) ganhou os mesmos 3 eixos de personalização das demais dobras de texto (cor de fundo, opacidade, cor de texto e "mostrar a faixa lateral", `corFundoLocalizacao`/`opacidadeFundoLocalizacao`/`corTextoLocalizacao`/`mostrarFaixaLocalizacao` em `Edicao`) — antes ficava com fundo fixo e faixa sempre visível, sem eixo de búzio porque a seção não usa esse marcador; seção "Comissões e Programas" (`/admin/edicoes/[id]/pagina/comissoes`, mesmo eixo de 7 campos de estilo de Publicações, `campo="Comissoes"`) tem o conteúdo gerido por um motor genérico de 3 níveis — Grupo → Lista → Item (models `GrupoConteudo`/`ListaConteudo`/`ItemConteudo`, sempre por edição), configurado numa única tela (`/admin/edicoes/[id]/grupos-conteudo`, seção `GRUPOS_CONTEUDO`, um formulário só salvando a árvore inteira, no molde de `ModalidadeSubmissaoForm`) — substituiu os antigos modelos hardcoded específicos de comissão (`TipoComissao`/`Comissao`/`ComissaoMembro`) e o catálogo global `ProgramaPosGraduacao` (que também exibia no rodapé via `Footer.jsx`; a listagem de programas migrou pra dentro desta dobra e saiu do rodapé); cada Grupo (ex. "Comissão Organizadora", "Programas de Pós-Graduação") tem N Listas, cada uma renderizada como um cartão em linha (eyebrow = nome do grupo, título = nome da lista, contagem de itens) que abre um modal com os Itens (nome + imagem/link opcionais, cobre tanto integrante de comissão quanto programa com logo e link); demais dobras da página ainda não iniciadas)
3. Inscrições (vagas, lista de espera, conflito de horários) — praticamente pronto (fluxo público completo em `(publico)/inscricao`: CPF primeiro — identifica conta existente por confirmação de e-mail, ou cadastro sem senha para CPF novo; inscrição geral no evento, uma por usuário por edição; seleção de atividades com `exigeInscricao` valida conflito de horário e aplica lista de espera quando lotada, tudo revalidado no servidor; e-mail de comprovante com HTML personalizado; quem já concluiu a inscrição geral pode voltar depois e se reidentificar pelo mesmo fluxo CPF+e-mail para se inscrever em mais atividades ou cancelar inscrições em atividades específicas já feitas — sem exigir login —, com a tela de confirmação mostrando um card da inscrição geral (evento/edição/período) e, aninhado, as inscrições em atividades específicas com opção de cancelar cada uma (confirmação inline, sem modal — site público não tem esse padrão) (`CardInscricaoConfirmada`); editar/cancelar a inscrição geral em si (não as atividades) continua exigindo login-based self-service, ainda não construído). Painel admin pronto — novo grupo "Inscrições" no menu da edição com duas telas: inscrições gerais (`InscricaoEdicao`, com busca de usuário cadastrado e exclusão que já remove em cascata as inscrições em atividades da mesma pessoa) e inscrições em atividades específicas (`InscricaoAtividade`, tabela única filtrável por atividade, com alternância de status confirmada/lista de espera e adicionar-manualmente-cria-a-inscrição-geral-se-faltar); etapa final de confirmação agora pode exibir uma mensagem de contribuição voluntária por edição (ex. pedido de PIX) — texto livre + ação opcional mutuamente exclusiva (link ou valor pra copiar), gerenciada em `/admin/edicoes/[id]/pagina/contribuicao` (`ContribuicaoForm`, seção `PAGINA_CONTRIBUICAO`) e renderizada em `CardContribuicao` só quando há texto cadastrado
4. Credenciamento por QR code — não iniciado
5. Área do participante — não iniciado
6. Testes — não iniciado
7. Deploy — não iniciado
8. Suporte — não iniciado

**Fase 2 (até fev/2027):**

9. Certificados — não iniciado
10. Migração Even3 — não iniciado
11. Anais — não iniciado
12. Site institucional — não iniciado (a listagem de "Programas de Pós-Graduação" que vivia no rodapé foi removida do `Footer.jsx` e migrou pra dentro da dobra "Comissões e Programas" da home — ver módulo 2 — deixando de ser um catálogo global e passando a ser conteúdo por edição, gerido pelo motor genérico Grupo/Lista/Item; demais dobras institucionais ainda não iniciadas)
13. Submissão de trabalhos — iniciado (modalidades e áreas temáticas agora são dinâmicas: cadastradas pelo gestor em Modalidades de submissão — modelo `ModalidadeSubmissao`/`AreaSubmissao`, por edição (área não tem coordenação/convidados — o antigo modelo `PessoaAreaSubmissao` foi removido do schema junto com a UI correspondente); área pode ser vinculada a uma ou mais atividades já cadastradas na mesma edição (FK `Atividade.areaSubmissaoId`, opcional, uma atividade pertence no máximo a uma área) — associação feita pelo lado da área, dentro do formulário de Modalidades de submissão (checklist de atividades no corpo expandido de `AreaSubmissaoLinha.jsx`); exclusão bloqueada nos três sentidos (atividade vinculada, área vinculada, modalidade com qualquer área vinculada), mesmo padrão de guard `count()`+`ErroHttp(409)` já usado em `atividades.service.js`; conteúdo que antes era hardcoded foi migrado pra edição corrente via script one-off; seção "Submissão" da home e as páginas `/submissao/[modalidade]`/`/submissao/[modalidade]/[area]` já consomem a API; fluxo de envio do trabalho em si pronto — botão "Realizar submissão" leva a `/submissao/[modalidade]/enviar`, com identificação passwordless só por e-mail (sem CPF: link mágico via `TokenVerificacao`/tipo `ENTRAR_SUBMISSAO`, sessão curta tipo Bearer nos moldes de `autenticarInscricao`, atalho automático para quem já está logado no site) e formulário com modalidade/área, título, resumo e referência bibliográfica em rich text — `CampoRichText` migrou de `components/interno` para `components/forms` para servir também o público, resumo permite negrito/itálico/lista/link/imagem (imagem embutida vira URL do GCS no backend, nunca fica base64 salvo) e referência só permite negrito — e autores: quem submete é sempre incluído automaticamente como autor principal, coautores são adicionados por e-mail num modal (`ModalAdicionarAutor`, autofill do nome se já houver conta) e associados a uma conta quando o e-mail bate, inclusive retroativamente se essa pessoa se cadastrar depois; novos models `Submissao`/`SubmissaoAutor`; tela admin de recebimento pronta (`/admin/edicoes/[id]/submissoes/recebimento`, seção `SUBMISSOES_RECEBIMENTO`) — duas abas: "Submissões" lista os trabalhos recebidos na edição com filtro em cascata modalidade→área e busca por título/autor, modal de detalhe somente leitura (autores, resumo e referência renderizados via `dangerouslySetInnerHTML`, já sanitizados na criação) e exclusão via `ModalConfirmacao`; "Configurações" é um atalho pra editar só `prazoInicio`/`prazoFim` das modalidades direto na tabela, sem sair da tela, reenviando o payload completo pro mesmo `PATCH` de Modalidades de submissão (`backend/src/services/submissoesAdmin.service.js`/`controllers`/`routes`, novo, mesmo padrão de `inscricoesAtividade.*`); avaliação (`SecaoAdmin.SUBMISSOES_AVALIACAO`) ainda não iniciada)

Ao concluir um módulo, atualize o status aqui nesta mesma sessão/PR.

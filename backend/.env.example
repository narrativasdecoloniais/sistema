# PostgreSQL (Railway ou local) — formato:
# postgresql://usuario:senha@host:porta/database
DATABASE_URL=

PORT=4000
FRONTEND_URL=http://localhost:3000

JWT_ACCESS_SECRET=troque-por-um-segredo-forte
JWT_REFRESH_SECRET=troque-por-outro-segredo-forte
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d

# SMTP (dev: deixe em branco para logar os e-mails no console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Narrativas <nao-responda@narrativas.unb.br>"
